{
  "name": "portfolix",
  "version": "0.1.0",
  "homepage": "http://kahdichienja.github.io/pofoliox",
  "private": true,
  "dependencies": {
    "web-vitals": "^2.1.2"
  },
  "scripts": {
    "deploy": "gh-pages -d /build",
    "push:build:deploy": "git push io main && npm run deploy"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}